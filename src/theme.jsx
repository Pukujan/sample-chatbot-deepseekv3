import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976D2", // Your primary color
    },
    common: {
      white: "#fff", // Define white under "common"
    },
  },
});

export default theme;
